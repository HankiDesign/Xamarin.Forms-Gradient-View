<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GradientControl"
             xmlns:controls="clr-namespace:GradientControl.Controls;assembly=GradientControl"
             x:Class="GradientControl.MainPage">
		<Grid>
				<!--<controls:GradientView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
																											Animating="{Binding IsAnimating}" Fps="{Binding Fps}" 
																											FramesPerTransition="{Binding FramesPerTransition}"/>-->
				<controls:ComplexGradientView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
				<!--<StackLayout Orientation="Vertical">
						<Label Text="Is Animating:" TextColor="White"/>
						<Switch IsToggled="{Binding IsAnimating}"	/>
						<Label Text="Frames Per Second:" TextColor="White" />
						<Slider Maximum="240" Minimum="1" Value="{Binding Fps}" />
						<Label Text="Frames Per Transition:" TextColor="White"/>
						<Slider Maximum="10000" Minimum="1" Value="{Binding FramesPerTransition}" />
				</StackLayout>-->
		</Grid>
</ContentPage>
